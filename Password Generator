#coding = utf-8
"""
    This is a easy python program producing reliable password for me.
    However the password generated by it is actually not too save.
    So I'm considering add something new to it.
"""
while(1):
    name = input("Input characters you want to transform:")
    calc = 1
    i = 0
    for i in range(0,len(name)):
       calc = calc * ord(name[i])
print(hex(calc))
